// 默认配置

config = {
  // 启动端口
  port: 3000,
  // mysql 配置
  mysql: {
    database: 'koa_demo',
    user: 'root',
    password: '123456',
    host: 'localhost',
    port: '3306'
  }
}

module.exports = config